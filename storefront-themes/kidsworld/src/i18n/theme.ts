// Theme-specific strings (manifest defaults, custom section copy).
// Translation agents add keys here; the file ships in the theme bundle.
import en from './locales/en/theme.json'
import ar from './locales/ar/theme.json'
export const themeResources = { en, ar }
